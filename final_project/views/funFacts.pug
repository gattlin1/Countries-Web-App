extends layout
block title
	title Country Fun Facts

block body
	h1 From the United States to Kiribati, Global Fun Facts
	h2 Study hard and test your knowledge with our quiz!
	.container
		form#search
			label Region #[input#region(type="text")]
			label Subregion #[input#subregion(type="text")]
			label CurrencyName #[input#currencyName(type="text")]
			label CurrencyCode #[input#currencyCode(type="text")]
			label Alpha3Code #[input#alpha3Code(type="text")]
			button.button(type="submit") Submit
	#results
	
	script(src="/js/facts.js")